server:
  port: 18888
  address: 0.0.0.0

spring:
  application:
    name: tools-service
  messages:
    basename: messages
    default-locale: zh-CN
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false

tools:
  ratelimit:
    enabled: true
    window-size: 60
    max-requests: 60

logging:
  level:
    com.tools: DEBUG
